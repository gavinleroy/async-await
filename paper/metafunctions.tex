\begin{tabular}{@{}l@{\quad}l@{}}
\toprule
Metafunction & Description \\
\midrule
$\mathsf{gensym} : () \to x$ & Create globally unique symbol \\
$\mathsf{task\_cancelled} : \sigma \times \ell \to \mathsf{Bool}$ & $\mathit{true}$ iff task $\ell$ or any ancestor is cancelled \\
$\mathsf{task\_alloc} : \sigma \to (\sigma, x)$ & Allocate a new task struct bound under gensymed name $x$ in $\sigma$ \\
$\mathsf{task\_add\_dependency} : \sigma \times \ell \times \ell \to \sigma$ & Add second label as dependency of first label's task \\
$\mathsf{task\_alloc\_dependency} : \sigma \times \ell \to (\sigma, x)$ & Allocate a fresh task as a dependency of $\ell$ \\
$\mathsf{task\_set\_done} : x \times e \to e$ & Evaluate $e$ and use value as $x$'s resolved success value \\
$\mathsf{task\_set\_failed} : x \times e \to e$ & Evaluate $e$ and use value as $x$'s resolved failure value \\
$\mathsf{task\_uncancel} : \sigma \times \ell \to \sigma$ & Remove cancellation from task $\ell$ in $\sigma$ \\
$\mathsf{task\_is\_completed} : e \to e$ & $\mathit{true}$ iff task is resolved (success or failure) \\
$\mathsf{task\_get\_result} : e \to e$ & Return task's final value; reraise if task failed \\
$\mathsf{task\_continue\_with} : v \times x \to e$ & Resume continuation bound by $x$ with settled value of task $v$ \\
$\mathsf{task\_get\_dependents} : e \to e$ & Return dependents (ancestors) of task $e$ evaluates to \\
$\mathsf{task\_cancel\_dependencies} : x \to e$ & Cancel task $x$ and its descendants \\
$\mathsf{task\_wait\_on\_dependencies} : x \to e$ & Wait for all dependencies of $x$ to settle \\
$\mathsf{task\_await\_dependencies} : x \to e$ & Wait for all dependencies of $x$ to settle, reraising exceptions \\
\bottomrule
\end{tabular}
