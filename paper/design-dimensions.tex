\section{Asynchronous Programming}\label{sec:designdims}

Async/await is a syntactic abstraction over a more complex asynchronous programming paradigm. How async/await syntax desugars into concurrency primitives is important to understand the runtime semantics of an async/await program.

In this section we will look at the architectural building blocks of asynchronous programming and the decisions language designers make when building the async/await abstraction.

\subsection{Async Building Blocks}

\begin{figure}
  \centering
  \includegraphics[width=0.98\textwidth]{./f/arch-overview.pdf}
  \caption{Cooperative Multitasking Architecture}
  \label{fig:architecture-diagram}
\end{figure}

An asynchronous programming system requires several key architectural components. \Cref{fig:architecture-diagram} provides a high-level diagrammatic view of an asynchronous runtime. \todo{Some description of the diagram.} In this section we explain each component and its role within the broader system.

\paragraph{\Coro (low-level control flow)} The most fundamental building block of asynchronous programming is a functions ability to suspend itself for later resumption. The base language must provide functionality equivalent to \textit{delimited continuations.} A majority of languages do not rely on continuations, but rather, rewrite asynchronous functions into a state machine. The expanded state machine code behaves like an \textit{asymmetric stackless coroutine,} to use the taxonomy provided by~\cite{moura_revisiting_2009}. General coroutines can yield values on suspension and accept values on resumption. For asynchronous programming the generated state machine does not need to yield values on suspension, but needs only to accept values on resumption (or access them at a shared location). To distinguish from general coroutines we will use the term \coro to refer to a coroutine with this limited ability.

\begin{figure}[htbp]
  \begin{minipage}[t]{0.42\textwidth}
\begin{lstlisting}[language=Rust]
async fn slow_print(msg):
  println!("{msg}")
  await sleep(Duration::millis(5))
\end{lstlisting}\vspace{2em}
\begin{lstlisting}[language=Rust]
enum SP:
  Init(msg),
  Waiting(fut)

fn slow_print(msg):
  SP::Init(msg)
\end{lstlisting}
  \end{minipage}\hfill%
  \begin{minipage}[t]{0.58\textwidth}
\begin{lstlisting}[language=Rust,firstnumber=7]
impl Future for SP:
  fn poll(self, ctx):
    loop:
      match *self:
        SP::Init(msg):
          let fut = sleep(Duration::millis(5))
          *self = SP::Waiting(fut)
        SP::Waiting(fut):
          if let Poll::Ready(()) = fut.poll(ctx):
            return Poll::Ready(())
          else:
            return Poll::Pending
\end{lstlisting}
  \end{minipage}
  \cprotect\caption{An example of how the Rust compiler transforms an async function into a state machine. The function code{} \ggnote{Using a pseduo-Rust syntax here and, even if we do want to keep an example here, we'll need something a bit more illustrative.}}
  \label{fig:state-machine-transform}
\end{figure}

\paragraph{\Task{} (async runtime object)} The \coro is a state machine representation of the users initial code, and the \task is the runtime object that tracks the state, e.g., the state of the state machine and other \Task{}s that are waiting on this \tasks value. There's an important distinction between the \coro and the \task, the former is a reification of the users code as an object, the latter is a pointer to that executing code. The \Task{} is registered with the current asynchronous runtime and will eventually be scheduled to run.

A \task represents one \coro that is registered in the runtime. To understand the behavior of an asynchronous system we will scrutinize not a single \task, but its relationship to other \tasks. We will describe \task relationships using a \textit{task tree.} The nodes in the task tree are the set of all executing \tasks in the runtime system. The task tree may be a reified data structure held explicitly by the runtime system, or may be held implicitly, e.g., through object pointers or registered callbacks. As we will discuss in \Cref{ssec:designdims}, how the task tree is constructed and destructed affects the runtime semantics.

\paragraph{Reactor (OS interface)} The reactor provides a communication layer between the runtime and the operating system. A fundamental assumption of cooperative multitasking is that the underlying operating system is capable of concurrent \abbr{io}. The reactor starts an \abbr{io} operation with the \abbr{os} and, when the \abbr{os} signals that the operation is done, will requeue the waiting \task with the returned value.

\paragraph{Executor (Thread interface)} The executor is responsible for scheduling queued \tasks on a thread to execute. The implementation of the Reactor and Executor are mostly a performance consideration, which we see as out of scope for this investigation. The Executor implementation may have implications on fairness and starvation, which we discuss in \Cref{sec:discussion}.

\begin{table}
  \begin{tabular}{lll}
    \toprule
    System            & \Coro               & \Task      \\
    \CSharp           & IAsyncStateMachine  & Task       \\
    JavaScript        & -                   & Promise    \\
    Swift             & -                   & Task       \\
    \midrule
    Rust              & Future              & -          \\
    Tokio (Rust)      & -                   & Task       \\
    SMOL (Rust)       & -                   & Task       \\
    \midrule
    Python            & Coroutine           & -          \\
    asyncio (Python)  & -                   & Task       \\
    Trio (Python)     & -                   & Task       \\
    \midrule
    Kotlin            & Coroutine           & Deferred   \\
    \Cpp{}20          & Coroutine           & Promise    \\
    Hack              & -                   & Awaitable  \\
    Nim               & -                   & Future     \\
    Dart              & -                   & Future     \\
    Zig               & -                   & Future     \\
    \FSharp           & -                   & Async      \\
    \bottomrule
  \end{tabular}
  \caption{An overview of asynchronous terminology used by modern programming languages. Many languages don't expose \coro types to developers, which means that a manual implementation is not possible using the exposed language \abbr{API}.}
  \label{tab:aa-terminology-overview}
\end{table}

\subsection{Design dimensions}\label{ssec:designdims}

Language designers must decide how the async/await syntax will expand into the set of asynchronous building blocks. The expansion is often influenced by patterns in the pre-existing base language as well as performance and memory expectations of the community.

In this section we provide the async/await design dimensions. The design dimensions map the async/await syntax to the runtime behavior of the desugared program. The design dimensions are: \textit{eagnerness,} \textit{extent,} \textit{references,} \textit{destruction,} \textit{propagation,} \textit{suspension,} and \textit{cancellation.}

\input{./dimensions/eagerness.tex}

\input{./dimensions/extent.tex}

\input{./dimensions/references.tex}

\input{./dimensions/destruct.tex}

\input{./dimensions/propagate.tex}

\input{./dimensions/suspend.tex}

\input{./dimensions/cancel.tex}
