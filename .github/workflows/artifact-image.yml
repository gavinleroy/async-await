# Build the artifact's Docker images for both architectures (native runners,
# no emulation), smoke-validate each by running the in-image fuzzer, push
# them to GHCR, and stitch a multi-arch manifest.
#
# Native arm64 runners (ubuntu-24.04-arm) are free for PUBLIC repositories.
# Release-tarball note: the images exceed GitHub's 2 GB per-release-asset
# limit, so GHCR is the distribution channel; the release can link to it.

name: artifact-image

on:
  release:
    types: [published]
  workflow_dispatch:

env:
  IMAGE: ghcr.io/${{ github.repository }}

permissions:
  contents: read
  packages: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - runner: ubuntu-24.04
            arch: amd64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      # The image closure + tarball need more than the runner's free disk;
      # drop the large preinstalled toolchains we don't use.
      - name: Free disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc \
                      /usr/local/.ghcup /usr/local/share/boost
          df -h /

      - uses: DeterminateSystems/nix-installer-action@v16

      - name: Build image with nix
        run: nix build -L ./artifact#image

      - name: Load and smoke-test
        # `fuzz -n 1 -r 2` runs the whole pipeline once per lane inside the
        # image — a real end-to-end validation on this architecture. A
        # genuine model mismatch found here fails the job by design.
        run: |
          docker load < result
          docker run --rm async-models-artifact:latest fuzz -n 1 -r 2

      - name: Push per-arch image to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          TAG="${{ github.event.release.tag_name || 'latest' }}"
          docker tag async-models-artifact:latest "$IMAGE:$TAG-${{ matrix.arch }}"
          docker push "$IMAGE:$TAG-${{ matrix.arch }}"

  manifest:
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - name: Create multi-arch manifest
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          TAG="${{ github.event.release.tag_name || 'latest' }}"
          docker buildx imagetools create -t "$IMAGE:$TAG" \
            "$IMAGE:$TAG-amd64" "$IMAGE:$TAG-arm64"
